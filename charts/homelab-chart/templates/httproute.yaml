{{- if .Values.httproute.create }}
{{- $route := merge (dict "name" (include "homelab-chart.fullname" .)) .Values.httproute }}
{{ include "homelab-chart.httproute" (dict "route" $route "root" .) }}
{{- end }}
{{- range .Values.httproutes }}
{{- if not .name }}
{{- fail "httproutes entry is missing required field: name" }}
{{- end }}
---
{{ include "homelab-chart.httproute" (dict "route" . "root" $) }}
{{- end }}
